# Benchmark Task - demo_workspace_inspection

## Input
Workspace:
- `examples/demo_workspace/demo_ws`

## Goal
The tool/agent should:
- identify this as a ROS2 workspace
- detect one package: `demo_nodes`
- classify it as `ament_python`
- detect launch/config assets
- recommend `build_workspace`
