# launch fixture: missing params file

Package prefix exists and launch file exists, but the expected params file is missing.

Expected diagnosis direction:
- package_found = true
- resolved launch path exists
- params_file_missing
- next action includes fix_params_path
