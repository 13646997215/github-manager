# launch fixture: valid prefix, valid launch, missing referenced asset

Expected diagnosis direction:
- package_found = true
- resolved launch path exists
- referenced_asset_missing
- recommend verify_referenced_assets before deeper runtime debugging
